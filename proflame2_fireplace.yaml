# =============================================================================
# Target board: generic ESP32 dev board + CC1101 breakout (e.g. ESP32-WROOM)
#
# Use this YAML if you wired a CC1101 433/315 MHz module to a plain ESP32 dev
# board over SPI. For the LilyGo T-Embed CC1101 (integrated radio + LCD +
# rotary encoder + battery), use proflame2_tembed.yaml instead.
#
# Required wiring (any free SPI-capable GPIOs work — pins below are examples):
#   CC1101 CS    → GPIO 21
#   CC1101 GDO0  → GPIO 22  (optional; only needed for on-device pairing)
#   SPI MOSI     → GPIO 23
#   SPI MISO     → GPIO 19
#   SPI SCLK     → GPIO 18
# Adjust the cs_pin / gdo0_pin / spi: section below to match your wiring.
# =============================================================================

# Shared proflame2 entity block (entity names, climate, heat-mode config,
# diagnostic sensors) lives in proflame2_common.yaml so both example
# configs stay in sync. Per-board pin map and serial/ECC stay below.
packages:
  base: !include proflame2_common.yaml

esphome:
  name: living-room-fireplace-remote
  friendly_name: Living Room Fireplace Remote

esp32:
  board: esp32dev
  cpu_frequency: 240MHz
  flash_size: 4MB
  framework:
    type: esp-idf
    version: recommended

logger:

api:
  encryption:
    key: !secret fireplace_remote_api_key

ota:
  - platform: esphome
    password: !secret fireplace_remote_ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Living-Room-Fireplace-Remote"
    password: !secret fireplace_remote_wifi_ap_password

captive_portal:

spi:
  clk_pin: GPIO18
  miso_pin: GPIO19
  mosi_pin: GPIO23

external_components:
  - source: github://dangerouslaser/proflame2-esp@main
    components: [proflame2]

# Per-board pin map + pairing identity. The shared entity block (power,
# pilot, aux, flame, fan, light, secondary_flame, pair, pair_confirm,
# pair_cancel, the diagnostic sensors, heat-mode entities, climate) is
# pulled in via packages: at the top of this file.
proflame2:
  cs_pin: GPIO21
  gdo0_pin: GPIO22
  serial_number: !secret fireplace_remote_serial_number
  # ECC pairing constants — must match your remote ↔ fireplace pairing.
  # See README "Pairing your remote" for how to derive these.
  ecc_constants:
    c1: 0x08
    d1: 0x0E
    c2: 0x0B
    d2: 0x07

sensor:
  # Climate's current_temperature source. Use an HA template helper as
  # indirection so the upstream sensor can be swapped without reflashing.
  - platform: homeassistant
    id: room_temp
    entity_id: sensor.fireplace_room_temperature
    filters:
      - lambda: 'return (x - 32.0f) * 5.0f / 9.0f;'  # °F → °C
  - platform: uptime
    type: seconds
    name: "Uptime Sensor"
  - platform: internal_temperature
    name: "Internal Temperature"
  - platform: wifi_signal
    name: "WiFi Signal dB"
    id: wifi_signal_db
    update_interval: 60s
    entity_category: "diagnostic"
  - platform: copy
    source_id: wifi_signal_db
    name: "WiFi Signal Percent"
    filters:
      - lambda: return min(max(2 * (x + 100.0), 0.0), 100.0);
    unit_of_measurement: "Signal %"
    entity_category: "diagnostic"
    device_class: ""

binary_sensor:
  - platform: status
    name: "Status"

button:
  - platform: restart
    name: "Restart"
    entity_category: config
  - platform: factory_reset
    name: "Factory Reset"
    id: Reset
    entity_category: config
  - platform: safe_mode
    name: "Safe Mode"
    internal: false
    entity_category: config

text_sensor:
  - platform: wifi_info
    ip_address:
      name: "IP Address"
      entity_category: diagnostic
    ssid:
      name: "Connected SSID"
      entity_category: diagnostic
    mac_address:
      name: "Mac Address"
      entity_category: diagnostic
  - platform: template
    name: "Last Restart"
    id: device_last_restart
    icon: mdi:clock
    entity_category: diagnostic
    device_class: timestamp

web_server:
  port: 80
  auth:
    username: admin
    password: !secret fireplace_remote_web_password

time:
  - platform: homeassistant
    id: homeassistant_time
